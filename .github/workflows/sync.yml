name: Sync Fork

on:
  workflow_dispatch: # on button click
  push:

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.4
        with:
          owner: REMServer
          base: master
          head: master
